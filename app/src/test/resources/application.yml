server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create

logging:
  level:
    root: INFO
    org.springframework: INFO